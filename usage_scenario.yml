name: "YouTube free" 
author: "Arturo + Clara"
description: Measuring youtube free
version: 1

services:
  browser-client:
    image: prueba-youtube
    build:
      context: .
    command: sleep infinity

flow:
  - name: PlayVideo
    container: browser-client
    commands:
      - type: console
        command: python measure_ytb_free.py
        note: "laying YouTube video"
